# base-cognos

## commands

```bash
mvn clean compile
mvn clean install
mvn clean test

mvn spotless:check
mvn spotless:apply

mvn -pl api spring-boot:run
```

## structure

```
cognos
├── api
├── core
└── tests
```